# Modules

Logical units (not a file listing). Each module owns a slice of behavior and clear dependencies.

## Contents

<!-- Numbered list, lexicographic: one link per module subfolder -->

1. [&lt;module-a&gt;](&lt;module-a&gt;/README.md)

## How to add a module

1. Create `docs/modules/<name>/README.md` with responsibilities, dependencies, and key entrypoints (link to source paths).
