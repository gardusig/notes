# Modules (top-level domains)

One **`docs/modules/<name>/README.md`** per **top-level folder** you want humans to navigate (e.g. `ci`, `release`, `templates`). Leaf detail can stay next to code or in deeper READMEs.

## Pattern

| Module | Responsibility |
| --- | --- |
| `<name>` | One line: what belongs here |

## Related

- [Module folder](module-domain-README.md) — template for a single domain page
